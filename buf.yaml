version: v2
modules:
  - path: pb
lint:
  use:
    - STANDARD
  except:
    # The existing schema predates these rules.
    - PACKAGE_DIRECTORY_MATCH
    - RPC_REQUEST_STANDARD_NAME
    - RPC_RESPONSE_STANDARD_NAME
    - RPC_REQUEST_RESPONSE_UNIQUE
breaking:
  use:
    - FILE
