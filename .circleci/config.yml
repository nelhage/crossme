version: 2
jobs:
  build:
    docker:
      - image: node:10.14.2-stretch
    steps:
      - checkout
      - run: curl https://install.meteor.com | /bin/sh
      - run: npm install
      - run: npm run lint
      - run: npm run test --allow-superuser
